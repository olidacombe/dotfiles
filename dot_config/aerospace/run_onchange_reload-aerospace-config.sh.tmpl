#!/usr/bin/env bash

# aerospace.toml hash: {{ include "dot_config/aerospace/aerospace.toml" | sha256sum }}

set -euo pipefail

if command -v aerospace &> /dev/null; then
    aerospace reload-config
else
